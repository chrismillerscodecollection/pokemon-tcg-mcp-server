[project]
name = "pokemon-tcg-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]==2.0.0b1",
]

[project.scripts]
pokemon_tcg_mcp_server = "pokemon_tcg_mcp_server.server:main"

[build-system]
requires = ["uv_build>=0.11.28,<0.12"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mypy>=2.3.0",
]