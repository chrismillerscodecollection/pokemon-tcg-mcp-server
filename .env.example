DB_URI=mongodburi

# Optional. Defaults shown.
HOST=0.0.0.0
PORT=8000
# MCP sessions live in process memory by default, so more than one replica
# behind a load balancer needs sticky routing on 'Mcp-Session-Id'. Set this to
# true instead and any replica can serve any request.
STATELESS_HTTP=false
